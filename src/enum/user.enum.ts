export enum UserRole {
    Student = 'student',
    Tutor = 'tutor',
    Admin = 'admin',
}

export enum UserGender {
    Male = 'male',
    Female = 'female',
}

export enum UserStatus {
    Active = 'active',
    Pending = 'pending',
    Suspended = 'suspended',
}
